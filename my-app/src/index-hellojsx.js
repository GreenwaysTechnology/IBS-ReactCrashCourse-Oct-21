import React from 'react'; // create dom elements
import ReactDOM from 'react-dom' // flush the component into javascript engine.

const  Heading= <h1>Hello React!</h1>;

ReactDOM.render(Heading,document.getElementById('root'));